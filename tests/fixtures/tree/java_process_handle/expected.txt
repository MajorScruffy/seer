input.java:2 fn process
  if (items.length == 0)
    return
  for (Item item : items)
    if (item.valid())
      input.java:14 handle(item)
        if (!item.ready())
          return
        JSON.toString(item)
        Files.write(path, data)
