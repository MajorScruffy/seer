input.ts:1 fn process
  if (items.length === 0)
    return
  for (const item of items)
    if (item.valid())
      input.ts:13 handle(item)
        if (!item.ready())
          return
        JSON.stringify(item)
        fs.writeFileSync(path, data)
