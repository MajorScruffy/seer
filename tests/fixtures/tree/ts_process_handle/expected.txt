fn process
  if (items.length === 0)
    return
  for (const item of items)
    if (item.valid())
      handle(item)
        if (!item.ready())
          return
        JSON.stringify(item)
        fs.writeFileSync(path, data)
